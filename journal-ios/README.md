# Journal (iOS)

SwiftUI reader for journal.heyitsmejosh.com. Pulls the live Atom feed
(`/feed.xml`), lists posts, renders each in a styled WebView. No bundled
content: empty state until the feed loads.

Build: `xcodegen generate && open JournalIOS.xcodeproj`
