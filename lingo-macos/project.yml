name: Lingo
options:
  bundleIdPrefix: com.heyitsmejosh
  deploymentTarget:
    macOS: "14.0"
  xcodeVersion: "16.0"
  minimumXcodeGenVersion: "2.38.0"

settings:
  base:
    SWIFT_VERSION: "5.9"
    DEVELOPMENT_TEAM: ""
    CODE_SIGN_IDENTITY: "-"

targets:
  Lingo:
    type: application
    platform: macOS
    sources:
      - path: Sources/LingoApp
        includes:
          - "**/*.swift"
      - path: Sources/LingoApp/Resources
        includes:
          - "**/*.json"
        buildPhase: resources
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.heyitsmejosh.lingo-macos
        INFOPLIST_GENERATION_MODE: GeneratedFile
        MARKETING_VERSION: "1.0.0"
        CURRENT_PROJECT_VERSION: "1"
        GENERATE_INFOPLIST_FILE: "YES"
        INFOPLIST_KEY_CFBundleDisplayName: "Lingo"

  LingoTests:
    type: bundle.unit-test
    platform: macOS
    sources:
      - path: Tests/LingoTests
    dependencies:
      - target: Lingo
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.heyitsmejosh.lingo-macos.tests
        GENERATE_INFOPLIST_FILE: "YES"
        TEST_HOST: "$(BUILT_PRODUCTS_DIR)/Lingo.app/Contents/MacOS/Lingo"
        BUNDLE_LOADER: "$(TEST_HOST)"
